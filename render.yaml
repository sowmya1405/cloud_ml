services:
  - type: web
    name: cloud-ml
    env: python
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app
    plan: free
    branch: main
    repo: https://github.com/sowmya1405/cloud_ml
